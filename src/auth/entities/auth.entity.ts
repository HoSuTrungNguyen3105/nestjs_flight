export class Auth {
  id: number;
  email: string;
  password: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  dateOfBirth: Date;
  role: string; // e.g., 'admin', 'user', etc.
}
